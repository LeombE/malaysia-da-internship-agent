# Prompt 09 — Rebuild From Zero Runbook

I need to rebuild or explain the entire project from zero for the Malaysia Data Analyst Internship Finder.

Use `AGENTS.md` and the `malaysia-da-internship-agent` skill.

Produce a step-by-step runbook covering:
1. Download/unzip starter repo.
2. Confirm project folder contains `src`, `config.yml`, `requirements.txt`.
3. Open CMD from the folder address bar.
4. Install Python if needed.
5. Create and activate `.venv`.
6. Install requirements.
7. Create `.env` from `.env.example` only once.
8. Add JSearch API key locally without quotes.
9. Run dry-run.
10. Create `.gitignore` and verify `.env`/`.venv` are excluded.
11. Install Git for Windows if needed.
12. `git init`, `git add`, `git status`, `git commit`.
13. Create private GitHub repo.
14. Add remote and push.
15. Add GitHub Secrets.
16. Enable read/write workflow permissions.
17. Run GitHub Actions manually.
18. Verify CSV outputs.
19. Troubleshoot common errors.

Make it beginner-friendly for Windows CMD.
Do not include real secrets.
